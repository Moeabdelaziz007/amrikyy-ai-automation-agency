---
alwaysApply: true
---

Axon + Quantum-Brain – Expert AI Project Rules & Workflow
🎯 الهدف النهائي

تمكين الـAI من تنفيذ، مراجعة، وتحسين الكود تلقائيًا ضمن مشروع Axon + Quantum-Brain مع التزام صارم بالمعايير العالمية، الأمان، والأداء.

1️⃣ Architecture & Workflow Rules

الفصل الصارم بين الطبقات:

Axon Hub (Next.js/TS): مسؤول عن UI وClient State فقط. لا يحتوي على أي منطق أعمال أو استعلامات مباشرة.

Quantum Brain (Python): مسؤول عن Core AI logic وAgent Execution وData Processing.

Universal Adapter: جسر وحيد للتواصل بين Hub وBrain. جميع الـAPI يجب أن تمر هنا.

خطوات العمل للـAI عند إضافة ميزة جديدة:

خطوة 1: تحليل الميزة → تحديد ما هو frontend وما هو backend.

خطوة 2: تصميم الواجهات البصرية كمكونات قابلة لإعادة الاستخدام.

خطوة 3: تحديد أي Agents جديدة مطلوبة وتسجيلها في /lib/agentRegistry.ts.

خطوة 4: التأكد من أن كل Agent يتبع IAgent interface ويُرجع Artifact object موحد.

API Rules:

RESTful، HTTP verbs قياسية (GET, POST, PUT, DELETE).

URL patterns: kebab-case /api/agent-details.

استخدام HTTP status codes الصحيحة لكل حالة.

2️⃣ Code Style & Conventions

Frontend Rules:

TypeScript strict mode + React 18+

Tailwind CSS + Shadcn/ui

State:

Server state: React Query/SWR

Global client state: Zustand فقط للحالات المشتركة

Local component state: useState / useReducer

Backend Rules:

Python 3.10+

PEP8 + type hints لكل متغير ودالة

Error Handling: استخدام custom exceptions فقط، لا generic Exception

Directory & Naming Conventions:

Components: /components/features/[feature-name]/

Shared Components: /components/shared/

Files: PascalCase.tsx, useCamelCase.ts, route.ts

خطوات عمل الـAI:

قبل إنشاء أي مكون أو دالة → تحقق من وجود مكون/وظيفة مماثلة لإعادة الاستخدام.

التزم بالمعايير اللغوية لكل لغة.

أضف Docstrings/TSDoc لكل دالة مع وصف مدخلات ومخرجاتها.

3️⃣ Database & Testing

Database Rules:

استخدام Prisma ORM فقط

أي SQL raw يجب توثيقه بوضوح

تأكد من indexing للحقول المستخدمة بكثرة

Testing Rules:

Unit tests: لكل دالة جديدة أو هُوك

Integration tests: لكل API endpoint حرج

Coverage ≥ 80%

خطوات عمل الـAI عند كتابة الكود:

إنشاء اختبار أولًا قبل الوظيفة إذا ممكن (TDD).

كتابة الكود مع الالتزام بالقواعد.

تشغيل الاختبارات → إذا فشل أي اختبار، إصلاح قبل الانتقال.

4️⃣ Security & Compliance

Environment Variables:

لا تضع أي secrets في الكود، استخدم .env فقط

Input Validation:

جميع البيانات الواردة من Hub يجب التحقق منها (XSS, SQLi)

Authentication & Authorization:

كل route حساس يجب حمايته والتأكد من صلاحية المستخدم

خطوات عمل الـAI:

عند التعامل مع مدخلات المستخدم، تحقق من نوع البيانات وطولها ونطاقها.

تسجيل أي محاولة وصول غير مصرح بها (Logging + Alerts).

5️⃣ Performance & Optimization

Lazy load مكونات كبيرة (next/dynamic)

React.memo لكل مكون مكلف يتلقى نفس props بشكل متكرر

Prisma queries مدعومة بـ Indexes

Optimized AI execution: استخدم caching عند الإمكان

خطوات عمل الـAI:

قياس زمن تحميل الصفحة أو زمن تنفيذ الدالة قبل التحسين.

تحديد bottleneck → تطبيق تحسينات.

6️⃣ AI Collaboration & Meta-Data Sharing

كل Agent يجب أن يشارك Meta-data قابلة للتخزين وإعادة الاستخدام مع الآخرين.

Self-healing: إذا فشل Agent، يمكن لوكيل آخر محاولة التنفيذ أو التعويض تلقائيًا.

خطوات عمل الـAI:

عند تنفيذ مهمة → سجل الـArtifact وMeta-data في Registry/Database.

عند فشل Agent → تحقق من سجل Meta-data لإعادة المحاولة أو تحسين التنفيذ.

7️⃣ Documentation & Git

Commit Messages: Conventional Commits (feat, fix, refactor, docs, chore)

Code Documentation: TSDoc/Python Docstring لكل دالة، class، أو component

خطوات عمل الـAI:

قبل أي push، أضف commit message مفصل يصف التغيير.

تأكد من وجود documentation لكل تغيير مهم.

8️⃣ AI Assistant Directives (Step-by-Step)

Plan First: قبل كتابة الكود، قدم خطة ملفات ومكونات جديدة.

Explain Changes: لكل refactor أو تعديل → اشرح سبب التحسين.

Strict Adherence: التزم بكل قاعدة من هذه الوثيقة.

Review & Self-Heal: بعد التنفيذ، تحقق من نتائج الاختبارات، قياسات الأداء، والتحقق الأمني.

Share Meta-Data: سجل كل نتائج التنفيذ لكل Agent لتسهيل التعاون المستقبلي.Axon + Quantum-Brain – Expert AI Project Rules & Workflow
🎯 الهدف النهائي

تمكين الـAI من تنفيذ، مراجعة، وتحسين الكود تلقائيًا ضمن مشروع Axon + Quantum-Brain مع التزام صارم بالمعايير العالمية، الأمان، والأداء.

1️⃣ Architecture & Workflow Rules

الفصل الصارم بين الطبقات:

Axon Hub (Next.js/TS): مسؤول عن UI وClient State فقط. لا يحتوي على أي منطق أعمال أو استعلامات مباشرة.

Quantum Brain (Python): مسؤول عن Core AI logic وAgent Execution وData Processing.

Universal Adapter: جسر وحيد للتواصل بين Hub وBrain. جميع الـAPI يجب أن تمر هنا.

خطوات العمل للـAI عند إضافة ميزة جديدة:

خطوة 1: تحليل الميزة → تحديد ما هو frontend وما هو backend.

خطوة 2: تصميم الواجهات البصرية كمكونات قابلة لإعادة الاستخدام.

خطوة 3: تحديد أي Agents جديدة مطلوبة وتسجيلها في /lib/agentRegistry.ts.

خطوة 4: التأكد من أن كل Agent يتبع IAgent interface ويُرجع Artifact object موحد.

API Rules:

RESTful، HTTP verbs قياسية (GET, POST, PUT, DELETE).

URL patterns: kebab-case /api/agent-details.

استخدام HTTP status codes الصحيحة لكل حالة.

2️⃣ Code Style & Conventions

Frontend Rules:

TypeScript strict mode + React 18+

Tailwind CSS + Shadcn/ui

State:

Server state: React Query/SWR

Global client state: Zustand فقط للحالات المشتركة

Local component state: useState / useReducer

Backend Rules:

Python 3.10+

PEP8 + type hints لكل متغير ودالة

Error Handling: استخدام custom exceptions فقط، لا generic Exception

Directory & Naming Conventions:

Components: /components/features/[feature-name]/

Shared Components: /components/shared/

Files: PascalCase.tsx, useCamelCase.ts, route.ts

خطوات عمل الـAI:

قبل إنشاء أي مكون أو دالة → تحقق من وجود مكون/وظيفة مماثلة لإعادة الاستخدام.

التزم بالمعايير اللغوية لكل لغة.

أضف Docstrings/TSDoc لكل دالة مع وصف مدخلات ومخرجاتها.

3️⃣ Database & Testing

Database Rules:

استخدام Prisma ORM فقط

أي SQL raw يجب توثيقه بوضوح

تأكد من indexing للحقول المستخدمة بكثرة

Testing Rules:

Unit tests: لكل دالة جديدة أو هُوك

Integration tests: لكل API endpoint حرج

Coverage ≥ 80%

خطوات عمل الـAI عند كتابة الكود:

إنشاء اختبار أولًا قبل الوظيفة إذا ممكن (TDD).

كتابة الكود مع الالتزام بالقواعد.

تشغيل الاختبارات → إذا فشل أي اختبار، إصلاح قبل الانتقال.

4️⃣ Security & Compliance

Environment Variables:

لا تضع أي secrets في الكود، استخدم .env فقط

Input Validation:

جميع البيانات الواردة من Hub يجب التحقق منها (XSS, SQLi)

Authentication & Authorization:

كل route حساس يجب حمايته والتأكد من صلاحية المستخدم

خطوات عمل الـAI:

عند التعامل مع مدخلات المستخدم، تحقق من نوع البيانات وطولها ونطاقها.

تسجيل أي محاولة وصول غير مصرح بها (Logging + Alerts).

5️⃣ Performance & Optimization

Lazy load مكونات كبيرة (next/dynamic)

React.memo لكل مكون مكلف يتلقى نفس props بشكل متكرر

Prisma queries مدعومة بـ Indexes

Optimized AI execution: استخدم caching عند الإمكان

خطوات عمل الـAI:

قياس زمن تحميل الصفحة أو زمن تنفيذ الدالة قبل التحسين.

تحديد bottleneck → تطبيق تحسينات.

6️⃣ AI Collaboration & Meta-Data Sharing

كل Agent يجب أن يشارك Meta-data قابلة للتخزين وإعادة الاستخدام مع الآخرين.

Self-healing: إذا فشل Agent، يمكن لوكيل آخر محاولة التنفيذ أو التعويض تلقائيًا.

خطوات عمل الـAI:

عند تنفيذ مهمة → سجل الـArtifact وMeta-data في Registry/Database.

عند فشل Agent → تحقق من سجل Meta-data لإعادة المحاولة أو تحسين التنفيذ.

7️⃣ Documentation & Git

Commit Messages: Conventional Commits (feat, fix, refactor, docs, chore)

Code Documentation: TSDoc/Python Docstring لكل دالة، class، أو component

خطوات عمل الـAI:

قبل أي push، أضف commit message مفصل يصف التغيير.

تأكد من وجود documentation لكل تغيير مهم.

8️⃣ AI Assistant Directives (Step-by-Step)

Plan First: قبل كتابة الكود، قدم خطة ملفات ومكونات جديدة.

Explain Changes: لكل refactor أو تعديل → اشرح سبب التحسين.

Strict Adherence: التزم بكل قاعدة من هذه الوثيقة.

Review & Self-Heal: بعد التنفيذ، تحقق من نتائج الاختبارات، قياسات الأداء، والتحقق الأمني.

Share Meta-Data: سجل كل نتائج التنفيذ لكل Agent لتسهيل التعاون المستقبلي.